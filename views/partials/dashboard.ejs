<nav id="dashboard-nav" class="navbar navbar-expand bg-light rounded-top">
    <div class="container-fluid justify-content-end">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link
                <% if (locals.profile) { %>
                    <%= "active" %>
                <% } %>" href="/dashboard/profile">Profile</a></li>
            <li class="nav-item">
                <a class="nav-link
                <% if (locals.history) { %>
                    <%= "active" %>
                <% } %>" href="/dashboard/history">History</a></li>
            <li class="nav-item">
                <a class="nav-link
                <% if (locals.search) { %>
                    <%= "active" %>
                <% } %>" href="/dashboard/search">Search</a></li>
        </ul>
    </div>
</nav>
